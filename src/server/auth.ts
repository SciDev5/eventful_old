// import { is_string } from "./data_verify";

// export async function auth_begin_password(username: unknown, password: unknown): Promise<boolean> {
//     if (!is_string(username) || !is_string(password)) { return false }



//     return true
// }
